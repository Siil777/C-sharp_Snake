1.Классы это типы данных они хранят данные
2.Класс point состоит из данных(для создания экземпляров этого класса) управление которыми происходит через функцию Draw
что позволяет в Main вызывать непосредственно класс с помощю Point p=new Point(); p.Draw();
3.Функция Draw помогает скрыть детали реалезации вывода координат.
4. Конструктор  будет вызываться при каждом создании новой точки
   Помогает сократить код

5. Инкапсуляция объеденение данных и функций в классе
6.list 
 //Объект класса лист хранит список целочисленных элементов
            List<Point> pList = new List<Point>();
            //добавили переменные p1,p2
            pList.Add(p1);
            pList.Add(p2);
7. Класс Линия - это список точек , туда входит конструктор метод который будет вызваться 
при создании. В конструкторе создаем пустой список: pList = new List<Point>();
так же записываем в этот класс функцию Draw() для вывода точек на экран в функции Draw() 
используем цикл foreach 
  public void Draw()
        {
            foreach (Point p in pList)
            {
                p.Draw();

            }
передать значения коструктору для возможности выводить линию любой длинны
public HorizontalTopLine(int xLeft,int xRight, int y, char sym)
Наследование-позволяет писать много классов на основе существующего
Draw будет вызываться из класса figure  потому что он определен в классе от каторого
наследуется

1.Инкапсуляция упакова данных в единый компонент в классы, так же инкапсуляция объеденяет данный код
манипулирующий этими данными, а так же, защещает от внешнего вмишательтва

2.Наследование поз. создавать новые классы которые повторно используют,расширяют, и
изменяют поведение определенных других классов

3.Полеморфизм - означает многообразие форм